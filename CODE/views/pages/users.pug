html
    head
        title User Page
        style.
            body {
                background-image: url(images/orderBackground.png);
                background-size: 100% 100%
            }
    body
        include ../partials/header
        div
            h1 List of users:
            br
            each user in users
                a(href="/users/" + user._id.toString() style="color: white; font-size: 130%") #{user.username}
                br